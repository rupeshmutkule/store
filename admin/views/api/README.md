# views/api

This folder was added per instruction to avoid modifying any existing backend files.

- products.route.js contains placeholder API routes.
- These routes are NOT active unless you mount them in app.js.

If you decide to wire them later, import the router and mount it under /api.
